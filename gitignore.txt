# Python cache & compiled files
__pycache__/
*.pyc

# Ignore environment files
venv/
.env
*.env

# Ignore data and output folders
# This prevents uploading large data files to GitHub!
# The .gitkeep files (or download_link.txt) will still be tracked.
bin files/*
csv/*
vid/*
resources/*

# But DO NOT ignore the .gitkeep files
# (or any text files you add inside)
!bin files/.gitkeep
!bin files/download_link.txt
!csv/.gitkeep
!vid/.gitkeep
!resources/.gitkeep